export type TResourceItem = {
    date: string;
    records: any[];
    resource: string,
    _id: string;
}

export type TResource = {
    date: string;
    count: number;
    resource: string,
    _id: string;
}